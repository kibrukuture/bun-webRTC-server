{
  "name": "backend",
  "module": "index.ts",
  "devDependencies": {
    "bun-types": "^0.6.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts"
  },
  "type": "module",
  "dependencies": {
    "nodemon": "^2.0.22"
  }
}
